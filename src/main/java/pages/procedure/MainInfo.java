package pages.procedure;

import core.BaseTest;
import org.openqa.selenium.support.PageFactory;

public class MainInfo extends BaseTest {

    public MainInfo() {
        PageFactory.initElements(driver,this);
    }
}
